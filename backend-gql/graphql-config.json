{
    "schema": "backend-gql/schema.graphql",
    "documents": "src/**/**/*.graphql",
    "extensions": {
        "endpoints": {
            "default": {
                "url": "http://localhost:4000/graphql"
            }
        }
    }
}